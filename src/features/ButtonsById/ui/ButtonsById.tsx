import React from 'react';

interface IGameSchema {
    _id: string,
    title: string,
    description: string,
    video: string,
    output: string
}

const ButtonsById = (props: IGameSchema) => {
    return (
        <>
        </>
    );
};

export default ButtonsById;